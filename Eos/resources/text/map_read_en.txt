ellipse.centerx           = getCenterX,real
ellipse.centery           = getCenterY,real
ellipse.radiusx           = getRadiusX,real
ellipse.radiusy           = getRadiusY,real

window.gridcolor       = getGridColor,color
window.backgroundcolor  = getBackgroundColor,color
window.height         = getHeight,real
window.width            = getWidth,real
window.top              = getTop,real
window.left             = getLeft,real
window.name              = getTitle,string
window.gridwidth     = getGridWidth,real

filledfigure.fillstyle     = getFillStyle,fillstyle
filledfigure.fillcolor   = getFillColor,color

circle.centerx              = getCenterX,real
circle.centery              = getCenterY,real
circle.radius              = getRadius,real

line.linewidth   = getLineWidth,real
line.linestyle           = getLineStyle,linestyle
line.linecolor               = getLineColor,color
line.x1                  = getX1,real
line.y1                  = getY1,real
line.x2                  = getX2,real
line.y2                  = getY2,real

square.side  = getSide,real

rectangle.height        = getHeight,real
rectangle.width           = getWidth,real
rectangle.top             = getTop,real
rectangle.left            = getLeft,real
rectangle.bottom            = getBottom,real
rectangle.right           = getRight,real

linefigure.bordercolor     = getLineColor,color
linefigure.borderstyle       = getLineStyle,linestyle
linefigure.borderwidth    = getLineWidth,real

textfield.textcolor            = getTextColor,color
textfield.textsize  = getTextSize,integer
textfield.font              = getFont,string
textfield.alignmentvertical     = getAlignmentVertical,alignment
textfield.alignmenthorizontal   = getAlignmentHorizontal,alignment
textfield.autoadjustsize = getAutoAdjust,boolean
textfield.hideborder            = getHideBorder,boolean

plotter.penx            = getPenX,real
plotter.peny            = getPenY,real
plotter.course               = getAngle,real
plotter.penvisible     = getPenVisible,boolean

figure.x                  = getX,real
figure.y                  = getY,real
figure.visible           = getVisible,real