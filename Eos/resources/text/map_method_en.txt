triangle.setcorners(6)           = setCorners,real,real,real,real,real,real
triangle.mirrorx(0)             = mirrorX
triangle.mirrory(0)             = mirrorY

ellipse.setcentery(1)            = setCenterY,real
ellipse.setcenterx(1)            = setCenterX,real
ellipse.setradiusy(1)            = setRadiusY,real
ellipse.setradiusx(1)            = setRadiusX,real
ellipse.setcenter(2)             = setCenter,real,real
ellipse.mitteylesen(0)           = getCenterY,real
ellipse.mittexlesen(0)           = getCenterX,real
ellipse.getradiusy(0)            = getRadiusY,real
ellipse.getradiusx(0)            = getRadiusX,real

window.move(2)                = move,real,real
window.gridon(0)              = turnGridOn
window.gridoff(0)             = turnGridOff
window.setgridcolor(1)        = setGridColor,color
window.getgridcolor(0)        = getGridColor,color
window.setbackgroundcolor(1)  = setBackgroundColor,color
window.getbackgroundcolor(0)  = getBackgroundColor,color
window.setheight(1)           = setHeight,real
window.getheight(0)           = getHeight,real
window.setwidth(1)            = setWidth,real
window.getwidth(0)            = getWidth,real
window.settop(1)              = setTop,real
window.gettop(0)              = getTop,real
window.setleft(1)             = setLeft,real
window.getleft(0)             = getLeft,real
window.setzoom(1)             = setZoom,real
window.setcenter(2)           = setCenter,real,real
window.setname(1)             = setTitle,string
window.getname(0)             = getTitle,string
window.setgridwidth(1)        = setGridWidth,real
window.getgridwidth(0)        = getGridWidth,real
window.draw(1)                = addFigure,figure
window.getmousex(0)           = getCursorX,real
window.getmousey(0)           = getCursorY,real
window.getmousedown(0)        = isCursorDown,boolean
window.getmouseclick(0)       = isCursorClick,boolean

figure.move(2)                  = move,real,real
figure.stretch(1)               = scale,real
figure.moveto(2)                = moveTo,real,real
figure.rotate(1)                = rotate,real
figure.rotatearound(3)          = rotateAround,real,real,real
figure.stretchat(3)             = scaleAt,real,real,real
figure.setvisible(1)            = setVisible,boolean
figure.getvisible(0)            = getVisible,boolean
figure.setx(1)                  = setX,real
figure.getx(0)                  = getX,real
figure.sety(1)                  = setY,real
figure.gety(0)                  = getY,real

filledfigure.setfillstyle(1)          = setFillStyle,fillstyle
filledfigure.setfillcolor(1)          = setFillColor,color
filledfigure.getfillstyle(0)          = getFillStyle,fillstyle
filledfigure.getfillcolor(0)          = getFillColor,color

group.setbordercolor(1)          = setLineColor,color
group.setborderstyle(1)          = setLineStyle,linestyle
group.setborderwidth(1)          = setLineWidth,real
group.setfillstyle(1)            = setFillStyle,fillstyle
group.setfillcolor(1)            = setFillColor,color
group.consume(1)                 = addFigure,figure
group.setcenter(2)               = setCenter,real,real
group.copy(1)                    = copyFigure,figure
group.mirrorx(0)                 = mirrorX
group.mirrory(0)                 = mirrorY

circle.setcenter(2)              = setCenter,real,real
circle.getcentery(0)            = getCenterY,real
circle.setcentery(1)             = setCenterY,real
circle.getcenterx(0)             = getCenterX,real
circle.setcenterx(1)             = setCenterX,real
circle.getradius(0)              = getRadius,real
circle.setradius(1)              = setRadius,real

line.setpoint1(2)              = setPoint1,real,real
line.setpoint2(2)              = setPoint2,real,real
line.setpoints(4)              = setPoints,real,real,real,real
line.setlinestyle(1)           = setLineStyle,linestyle
line.getlinestyle(0)           = getLineStyle,linestyle
line.setlinewidth(1)           = setLineWidth,real
line.getlinewidth(0)           = getLineWidth,real
line.setx1(1)                  = setX1,real
line.getx1(0)                  = getX1,real
line.setx2(1)                  = setX2,real
line.getx2(0)                  = getX2,real
line.sety1(1)                  = setY1,real
line.gety1(0)                  = getY1,real
line.sety2(1)                  = setY2,real
line.gety2(0)                  = getY2,real
line.setcolor(1)               = setLineColor,color
line.getcolor(0)               = getLineColor,color

square.setside(1)     = setSide,real
square.setlefttop(2)  = setLeftTop,real,real
square.getside(0)     = getSide,real

rectangle.setheight(1)          = setHeight,real
rectangle.setwidth(1)             = setWidth,real
rectangle.setleft(1)              = setLeft,real
rectangle.setright(1)             = setRight,real
rectangle.settop(1)               = setTop,real
rectangle.setbottom(1)              = setBottom,real
rectangle.setlefttop(2)          = setLeftTop,real,real
rectangle.setrightbottom(2)        = setRightBottom,real,real
rectangle.setcorners(4)              = setCorners,real,real,real,real
rectangle.getheight(0)           = getHeight,real
rectangle.getwidth(0)              = getWidth,real
rectangle.getleft(0)               = getLeft,real
rectangle.getright(0)              = getRight,real
rectangle.gettop(0)                = getTop,real
rectangle.getbottom(0)               = getBottom,real

linefigure.setbordercolor(1)          = setLineColor,color
linefigure.setborderstyle(1)          = setLineStyle,linestyle
linefigure.setborderwidth(1)          = setLineWidth,real
linefigure.getbordercolor(0)          = getLineColor,color
linefigure.getborderstyle(0)          = getLineStyle,linestyle
linefigure.getborderwidth(0)          = getLineWidth,real

textfield.appendline(1)     = appendLine,string
textfield.deleteline(0)     = deleteLine
textfield.settextcolor(1)       = setTextColor,color
textfield.gettextcolor(0)        = getTextColor,color
textfield.settextsize(1)      = setTextSize,integer
textfield.gettextsize(0)       = getTextSize,integer
textfield.setfont(1)         = setFont,string
textfield.getfont(0)          = getFont,string
textfield.setalignmentvertical(1)      = setAlignmentVertical,alignment
textfield.getalignmentvertical(0) = getAlignmentVertical,alignment
textfield.setautoadjustsize(1) = setAutoAdjust,boolean
textfield.getautoadjustsize(0) = getAutoAdjust,boolean
textfield.setalignmenthorizontal(1)    = setAlignmentHorizontal,alignment
textfield.getalignmenthorizontal(0)    = getAlignmentHorizontal,alignment
textfield.sethideborder(1)       = setHideBorder,boolean
textfield.gethideborder(0)        = getHideBorder,boolean

grafic.loadimage(1)               = loadImage,string
grafic.stretchimage(0)           = stretchImage
grafic.cutimage(0)        = cutImage
grafic.fitimage(0)          = fitImage

plotter.pendown(0)                = startPlotting
plotter.penup(0)               = stopPlotting
plotter.turnleft(1)            = turnLeft,real
plotter.turnright(1)           = turnRight,real
plotter.left(1)                  = turnLeft,real
plotter.right(1)                 = turnRight,real
plotter.setpenxy(2)          = movePenTo,real,real
plotter.back(1)            = moveBackward,real
plotter.forward(1)                    = moveForward,real
plotter.setcolor(1)            = setLineColor,color
plotter.getcolor(0)             = getLineColor,color
plotter.setpenx(1)          = setPenX,real
plotter.getpenx(0)           = getPenX,real
plotter.setpeny(1)          = setPenY,real
plotter.getpeny(0)           = getPenY,real
plotter.setcourse(1)             = setAngle,real
plotter.getcourse(0)              = getAngle,real
plotter.setpenvisible(1)   = setPenVisible,boolean
plotter.getpenvisible(0)    = getPenVisible,boolean
plotter.setlinewidth(1)  = setLineWidth,real
plotter.setlinestyle(1)          = setLineStyle,linestyle
plotter.setfillstyle(1)       = setFillStyle,fillstyle
plotter.setfillcolor(1)     = setFillColor,color
plotter.setcenter(2)            = setCenter,real,real
plotter.clearall(0)             = clearAll

color.getgreen(0)           = getGreen,integer
color.getred(0)             = getRed,integer
color.getblue(0)            = getBlue,integer
color.getalpha(0)           = getAlpha,integer
color.brighter(0)           = brighter,color
color.darker(0)             = darker,color